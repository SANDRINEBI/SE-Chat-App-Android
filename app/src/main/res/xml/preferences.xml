<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <SwitchPreference
        android:defaultValue="true"
        android:key="notifyPhone"
        android:title="@string/pref_phone_notifications_title"
        android:summary="@string/pref_phone_notifications_summary"/>

    <SwitchPreference
        android:defaultValue="false"
        android:key="notifyWatch"
        android:title="@string/pref_watch_notifications_title"
        android:summary="@string/pref_watch_notifications_summary"/>

    <Preference
        android:title="@string/logout_title"
        android:summary="@string/logout_summary"
        android:key="logoutButton"
        />

</PreferenceScreen>
